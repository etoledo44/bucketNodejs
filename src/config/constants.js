const path = require("path");

const CONSTANTS = {
  uploadDir: path.join(__dirname, "..", "..", "documents"),
  uploadDirImages: path.join(__dirname, "..", "..", "documents", "images"),
};

module.exports = CONSTANTS;
